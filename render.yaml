services:
  - type: web
    name: media-bot
    runtime: python
    plan: free
    autoDeploy: true
    buildCommand: |
      apt-get update && apt-get install -y ffmpeg
      pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: BOT_TOKEN
        sync: false
